# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This repository is a **new, empty project** — an AI agent for quantum computing. No source code has been committed yet. The remote origin is `https://github.com/lggyx/AI-Agent-for-Quantum-Computing.git`.

The project structure and tooling will be defined as development progresses. This file should be updated with:

- Build, test, and lint commands once a language/framework is chosen
- Project architecture once initial scaffolding is in place
- Any domain-specific conventions for quantum computing code

## Repository Status

- **Branch:** `main` (no commits yet)
- **Remote:** origin set to GitHub
- **Language/framework:** TBD

## First-Time Setup

When the project has code, run any setup commands from the repository root (e.g., `npm install`, `pip install -r requirements.txt`, etc.).